name: Build and Push Docker Image

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - 'feature/*'

permissions:
  packages: write
  contents: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Optional: build frontend
      - name: Setup Node & Build
        run: |
          npm install -g pnpm
          pnpm install || pnpm init -y
          pnpm run build || echo "placeholder build executed"

      # Login to GHCR
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Generate lowercase image name
      - name: Set image tag
        id: vars
        run: |
          echo "IMAGE_TAG=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/${GITHUB_REPOSITORY##*/,,}:latest" >> $GITHUB_ENV
          echo "IMAGE_SHA_TAG=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/${GITHUB_REPOSITORY##*/,,}:${GITHUB_SHA}" >> $GITHUB_ENV

      # Build & Push Docker image
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ env.IMAGE_TAG }},${{ env.IMAGE_SHA_TAG }}
